# 변경 히스토리 (Modification History)

본 파일은 지식 관리 시스템(Second Brain)의 기능 추가 및 수정 사항을 기록하는 파일입니다.

## [2026-04-16] - 현재 세션

### 추가된 기능
- **AI 태그 클리너 (Tag Management)**
    - LLM을 사용하여 유사한 태그를 자동으로 그룹화하는 백엔드 로직 구현.
    - 사용자가 최종 병합 태그를 직접 수정할 수 있는 UI 구현.
    - 볼트 전체 파일에서 태그를 일괄 변경하는 병합 기능 구현.
- **폴더 포맷 (Folder Cleanup)**
    - 특정 폴더 내의 파일 및 하위 폴더를 일괄 삭제하는 기능 구현.
    - 삭제 전 하위 폴더 개수, 파일 수, 총 용량 정보를 조회하여 사용자에게 확인시켜주는 기능 추가.
    - 시스템 폴더(Inbox, Sources, MOC 등) 삭제 방지 안전장치 적용.

### 개선 및 수정 사항
- **프론트엔드 UI/UX 수정 누락 (캐시 이슈) 해결**
    - `index.html` 파일 내 CSS 및 JS 소스의 쿼리 스트링(버전)을 업데이트하여, 브라우저가 이전 캐시가 아닌 최신 스크립트 파일을 강제로 불러오도록 처리.
- **프로그레스바 시각화 개선**
    - 백엔드 처리 단계 사이에 인위적인 지연시간(`asyncio.sleep`)을 추가하여 진행 상황이 눈에 보이도록 수정.
    - 프론트엔드 CSS 트랜지션 시간을 늘려 프로그레스바 애니메이션을 부드럽게 개선.
    - 프로그레스바 높이를 4px에서 8px로 늘려 가시성 확보.
- **백엔드 서버 안정화**
    - 코드 수정 중 발생한 파이썬 문법 오류(들여쓰기, f-string 이스케이프) 수정.
    - 서버 실행 방식 안내 및 환경 설정 최적화.

### 테스트 결과
- URL 스크래핑 시 프로그레스바가 단계별(추출 -> 분석 -> 기록)로 점진적으로 채워지는 것 확인.
- AI 태그 클리너 분석 결과에서 태그 수정 및 병합 실행이 정상 작동함 확인.
- 폴더 포맷 시 폴더 내부 정보가 올바르게 표시되고 실제 삭제가 수행됨 확인.
